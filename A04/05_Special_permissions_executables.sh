#Working on rocky

#The shadow file
ls -l /etc/shadow

#Cat will no work
cat /etc/shadow

#The passwd command
ls -l /usr/bin/passwd

#Another example
ls -l $(tty)

#The write command
ls -l /usr/bin/write